---
name: discover
description: Use when defining a new product idea - purpose, business plan, and usage scenarios - via an adaptive interview (3 seeds, then generated questions) persisted to docs/design/mockup-interview.md until the user stops or nothing is left, then a full traceable HTML mockup in docs/design/mockup/. Part of the archdesign plugin.
---

# Archdesign: discover

1. Read `${CLAUDE_PLUGIN_ROOT}/skills/generate/SKILL.md` — its hard
   rules and style references govern this run.
2. Read `${CLAUDE_PLUGIN_ROOT}/skills/generate/references/subcommands.md`
   and execute the `## discover` protocol exactly.
